# Path to your Firebase service account JSON (download from Firebase Console).
# Used only by update-firebase-projects.js — never commit the real key file.
# Get it: Firebase Console → Project Settings → Service Accounts → Generate new private key.
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
